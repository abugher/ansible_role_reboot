# m     h       dom     mon     dow     user    command
{% if skip_reboot is defined %}#{% endif %}{{ reboot_minute }}      {{ reboot_hour }}      *       *       *       root    /usr/sbin/shutdown -r +1 "scheduled reboot"
